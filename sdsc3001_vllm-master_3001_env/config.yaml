server:
  model: "/home/mkchua2/Miki/SDSC3001/Qwen3_8B_gguf/qwen3-8b.gguf"  # Path to local AWQ quantized model
  tensor_parallel_size: 2  # Number of GPUs to use for tensor parallelism (worker)
  gpu_memory_utilization: 0.7
  max_model_len: null  # Use null for default
  dtype: "float16"
  trust_remote_code: true
  enforce_eager: true
  disable_custom_all_reduce: true
  enable_chunked_prefill: false
  host: 0.0.0.0
  port: 5000
  override_generation_config: '{"temperature": 0.0, "top_p": 1.0}'
  # quantization: "awq"  # Specify AWQ quantization method
